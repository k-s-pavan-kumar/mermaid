// Placeholder until you run `npm run supabase:types` against a real project
// (requires the Supabase CLI and SUPABASE_PROJECT_ID). Until then this keeps
// src/lib/supabase/*.ts compiling; it isn't used at all while
// DATA_PROVIDER=local.
export type Database = Record<string, unknown>;
